/** Legacy no-op — window chrome is handled by the custom TitleBar. */
export function syncTitleBarOverlay(
  _playerOpen?: boolean,
  _opts?: { chromeHidden?: boolean }
): void {
  /* intentionally empty */
}
